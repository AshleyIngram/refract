[package]
name = "refract-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "refract-cli"
path = "src/main.rs"

[dependencies]
refract = { path = "../refract" }
refract-scenes = { path = "../refract-scenes" }
